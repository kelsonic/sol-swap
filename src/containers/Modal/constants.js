// Relative imports.
import ModalWallets from "../../components/ModalWallets";

// Define modal types.
export const MODAL_WALLETS = "MODAL_WALLETS";

// Create a lookup table of modal types and the components they link to.
export const MODAL_TYPES = {
  [MODAL_WALLETS]: ModalWallets,
};
